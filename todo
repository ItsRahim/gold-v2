Cleanup on the exceptions being thrown, when they're being caught and the logs
Implement ELK, prometheus, grafana
Remove the GRPC code, libraries. Possibly H2
Improve exception handling and logging in pricing-service
Email verification add button to click and verify email - future work