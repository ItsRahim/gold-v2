spring:
  application:
    name: email-service

  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: none

eureka:
  client:
    enabled: false
